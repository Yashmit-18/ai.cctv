# Runtime
ultralytics>=8.0
torch>=2.0
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0

# Face Recognition / Dynamic Identification (Phase 6)
insightface>=0.7.3
onnxruntime>=1.15
scikit-learn>=1.0       # cosine-similarity helpers (sklearn.metrics)
numpy>=1.24

# Web Dashboard (Phase 4)
streamlit>=1.30
plotly>=5.18

# Optional (highly recommended): CPU-only PyTorch wheels are smaller.
# Windows users can also install torch once separately:
#   python -m pip install torch

# OpenCV (HEADLESS) --- MUST be declared AFTER insightface and installed
# LAST so it wins the single `cv2` package directory on Streamlit Cloud.
# insightface's own `Requires-Dist: opencv-python` pulls the GUI build,
# which needs libGL.so.1 that Cloud images do not ship; importing that cv2
# crashes with an ImportError.  Installing opencv-python-headless last makes
# the headless build overwrite the GUI one, so `import cv2` works on Cloud.
# The dashboard runtime only ever uses headless-compatible cv2 APIs
# (VideoCapture / RTSP / image ops - never imshow or GUI windows).
opencv-python-headless>=4.9

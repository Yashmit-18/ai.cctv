[pytest]
markers =
    simulation: deterministic virtual-camera / scenario simulation tests (Phase 36)
    stress: load / database / evidence / alert stress tests (Phase 36)
    soak: long-running soak tests (Phase 36) - may be slow, excluded by default config
    website: dashboard / website / admin-panel tests (Phase 38)
testpaths = tests